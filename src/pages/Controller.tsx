import { Card } from 'antd';
import React from 'react';

const Controller: React.FC = () => {
  return (
    <div>
      <Card>
        <h1> Controller </h1>
        TODO: add a simple place to test streaming / non-streaming API endpoints
      </Card>
    </div>
  );
};

export default Controller;
